# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.env

# Transient borrow-rate data (refetched live; history accrues locally).
# Price CSVs under cache/ ARE committed: they are the seed data the deployed
# app reads, since Polygon's free-tier rate limit makes cold fetches fail.
cache/borrow_rates.csv
cache/borrow_history.csv
__pycache__/
.venv/
*.pyc
*.txt

# stray nested caches
**/cache/borrow_rates.csv
**/cache/borrow_history.csv
src/pages/cache/
